These dependencies can optionally be used at runtime depending on your RestFB distribution of choice.

There are 3 different RestFB distributions:

1. restfb-jul-x.y.jar (Uses java.util.logging, no dependencies)
2. restfb-log4j-x.y.jar (Has a dependency on Log4j)
3. restfb-jcl-x.y.jar (Has a dependency on Commons-Logging)